# Sprint 37: Оптимизация, улучшение UX и исправление тестов

**Даты**: 18-24 ноября 2025
**Статус**: 🟢 Запланирован

---

## 🎯 Цели спринта

### Основная цель
Повысить производительность, улучшить пользовательский опыт в ключевых областях и начать работу по сокращению технического долга путем исправления нестабильных тестов.

### Конкретные задачи

#### 1. Оптимизация производительности `ProjectBrowser` (P1)
- **Проблема**: `ProjectSelectorDialog` может работать медленно при большом количестве проектов (>100), так как список не виртуализирован.
- **Задача**: Интегрировать `react-virtual` или аналогичную библиотеку для виртуализации списка проектов в `ProjectBrowser.tsx`.
- **Критерии приемки**:
  - Список проектов в диалоге "Open Project" использует виртуализацию.
  - Производительность остается высокой даже с 1000+ моковыми проектами.

#### 2. Улучшение UX в `ProjectMenu` (P2)
- **Проблема**: Функция "Save As" в `ProjectMenu` просто сохраняет копию без запроса нового имени, что неинтуитивно.
- **Задача**: Создать новый диалог, который запрашивает у пользователя имя нового проекта при нажатии на "Save As".
- **Критерии приемки**:
  - При клике на "Save As" появляется диалоговое окно с полем для ввода имени.
  - Проект сохраняется с новым именем.

#### 3. Рефакторинг боковой панели `WorkspaceLayout` (P2)
- **Проблема**: Боковая панель в `WorkspaceLayout.tsx` открывается при наведении, что может мешать пользователям.
- **Задача**: Изменить поведение боковой панели так, чтобы она открывалась и закрывалась по клику на специальную иконку.
- **Критерии приемки**:
  - Боковая панель по умолчанию свернута.
  - Клик по иконке "бургер" или аналогичной плавно разворачивает панель.
  - Клик по иконке "закрыть" или повторный клик по "бургеру" сворачивает панель.

#### 4. Исправление тестов `useAudioRecorder` (Технический долг)
- **Проблема**: Тесты в `useAudioRecorder.test.ts` нестабильны и часто падают, что создает шум при запуске CI.
- **Задача**: Проанализировать и исправить 3 падающих теста, связанных со стартом, остановкой и отслеживанием времени записи.
- **Критерии приемки**:
  - Все тесты в `useAudioRecorder.test.ts` успешно проходят.
  - Исправления не нарушают логику работы хука.
