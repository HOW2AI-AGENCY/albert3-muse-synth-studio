# 🚀 Инструкция по применению миграции лайков для версий

**Файл миграции:** `supabase/migrations/manual/0002_add_track_version_likes_system.sql`  
**Project ID:** `qycfsepwguaiwcquwwbw`  
**Приоритет:** Высокий  
**Время выполнения:** ~2-5 минут

---

## 📋 Пошаговая инструкция

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите на https://supabase.com/dashboard/project/qycfsepwguaiwcquwwbw
2. Войдите в систему, если требуется
3. Убедитесь, что вы находитесь в правильном проекте

### Шаг 2: Откройте SQL Editor

1. В левом меню найдите раздел **SQL Editor**
2. Нажмите кнопку **New Query** для создания нового запроса

### Шаг 3: Скопируйте и выполните SQL

1. Откройте файл `supabase/migrations/manual/0002_add_track_version_likes_system.sql`
2. Выделите **весь** содержимое файла (Ctrl+A)
3. Скопируйте (Ctrl+C)
4. Вставьте в SQL Editor в Supabase Dashboard (Ctrl+V)
5. Нажмите кнопку **Run** (или F5)

### Шаг 4: Проверьте результаты

После выполнения в консоли должны появиться NOTICE-сообщения:

```
NOTICE: Created table: track_version_likes
NOTICE: Created indexes for track_version_likes
NOTICE: Added like_count column to track_versions
NOTICE: Created trigger: update_version_likes_count_trigger
NOTICE: Enabled RLS for track_version_likes
NOTICE: Created 3 RLS policies
NOTICE: ===== ✅ ALL CHECKS PASSED =====
```

Если вы видите `ALL CHECKS PASSED` - миграция применена успешно!

### Шаг 5: Регенерируйте TypeScript типы

Откройте терминал в корне проекта и выполните:

```bash
npx supabase gen types typescript --project-id qycfsepwguaiwcquwwbw > src/integrations/supabase/types.ts
```

Эта команда обновит TypeScript-типы для новой таблицы `track_version_likes`.

### Шаг 6: Сообщите об успешном применении

После выполнения всех шагов напишите мне: **"Миграция применена, типы регенерированы"**

Я продолжу работу:
- Доб авлю методы в `LikesService`
- Создам хук `useTrackVersionLike`
- Обновлю компоненты
- Протестирую функциональность

---

## 🔍 Как проверить, что все работает

Выполните в SQL Editor:

```sql
-- 1. Проверка таблицы
SELECT table_name, column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'track_version_likes';
-- Ожидается: 4 строки (id, user_id, version_id, created_at)

-- 2. Проверка поля like_count
SELECT column_name, data_type, column_default
FROM information_schema.columns 
WHERE table_name = 'track_versions' 
  AND column_name = 'like_count';
-- Ожидается: 1 строка (like_count, integer, 0)

-- 3. Проверка триггера
SELECT trigger_name, event_manipulation, event_object_table
FROM information_schema.triggers 
WHERE event_object_table = 'track_version_likes';
-- Ожидается: 1 строка (update_version_likes_count_trigger)

-- 4. Проверка политик
SELECT policyname, cmd, roles
FROM pg_policies 
WHERE tablename = 'track_version_likes';
-- Ожидается: 3 строки (политики для INSERT, SELECT, DELETE)

-- 5. Проверка счетчиков в существующих версиях
SELECT COUNT(*) as total_versions,
       COUNT(like_count) as versions_with_count
FROM track_versions;
-- total_versions должен равняться versions_with_count
```

---

## ⚠️ Troubleshooting

### Ошибка: "relation track_version_likes already exists"
**Решение:** Таблица уже создана ранее. Это нормально, миграция пропустит создание.

### Ошибка: "column like_count already exists"
**Решение:** Поле уже добавлено. Миграция продолжит выполнение.

### Ошибка: "permission denied"
**Решение:** Убедитесь, что у вас есть права администратора проекта в Supabase.

### Ошибка при регенерации типов
**Решение:** 
1. Убедитесь, что Supabase CLI установлен: `npm install -g supabase`
2. Проверьте правильность project-id в команде
3. Убедитесь, что у вас есть доступ к проекту

---

## 🎯 После успешного применения

Вы сможете:
- ✅ Ставить лайки на конкретные версии треков
- ✅ Видеть разные счетчики для разных версий
- ✅ Переключаться между версиями с сохранением лайков
- ✅ Получать real-time обновления счетчиков

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи в SQL Editor (должны быть NOTICE-сообщения)
2. Выполните валидационные запросы из раздела "Как проверить"
3. Опишите ошибку детально, и я помогу

---

**Готовы к применению!** 🚀
