# 🎛️ Prompt DJ — Руководство пользователя

## Обзор

**Prompt DJ** — экспериментальная функция для реалтайм-микширования музыки с помощью Gemini Lyria Live Music API. Позволяет смешивать до 16 промптов в реальном времени с регулируемыми весами.

## ✨ Возможности

- 🎚️ **16 промпт-контроллеров** с индивидуальными весами (0.0 - 1.0)
- 🔄 **Реалтайм-микширование** — изменения применяются мгновенно
- 🎨 **Визуализация аудио** — пульсирующие индикаторы на активных промптах
- 🎧 **WebSocket стриминг** — бесшовное воспроизведение через Web Audio API
- ⚡ **Низкая латентность** — 2-секундный буфер для стабильности

## 🚀 Как использовать

### 1. Доступ к функции

Перейдите по адресу: `/workspace/prompt-dj` или нажмите **"Prompt DJ"** в боковой навигации.

### 2. Запуск сессии

1. **Настройте промпты** (опционально):
   - Каждый из 16 контроллеров имеет текстовое поле для промпта
   - Примеры: `"Epic orchestral symphony"`, `"Ambient electronic soundscape"`

2. **Настройте веса**:
   - Используйте ползунки для установки веса (0 = выкл, 1 = макс)
   - По умолчанию случайным образом активированы 3 промпта

3. **Нажмите "Запустить"**:
   - Система установит WebSocket соединение с Gemini Lyria
   - Статус изменится на "Подключение..." → "Загрузка..." → "Играет"

### 3. Реалтайм-микширование

**Во время воспроизведения:**
- 🎛️ Двигайте ползунки для изменения весов
- ✍️ Меняйте текст промптов
- 🎨 Наблюдайте пульсирующие индикаторы (отображают уровень аудио)

**Визуальные индикаторы:**
- **Зелёный пульсирующий статус** — музыка генерируется
- **Светящиеся рамки** — активные промпты (weight > 0)
- **Красная рамка + иконка** — промпт отфильтрован Gemini

### 4. Управление громкостью

Используйте общий слайдер громкости в верхней части интерфейса (0-100%).

### 5. Остановка сессии

Нажмите **"Стоп"** для завершения WebSocket соединения.

## 🏗️ Техническая архитектура

### Backend (Edge Function)

**Файл:** `supabase/functions/prompt-dj-lyria-stream/index.ts`

**Основные роуты:**
- `/connect` — создание сессии и инициализация
- `/stream` — WebSocket соединение для стриминга аудио
- `/update-prompts` — HTTP эндпоинт для обновления промптов (fallback)

**Формат сообщений:**

```typescript
// Client → Server
{
  type: 'prompts.update',
  prompts: [
    { text: "Jazz piano", weight: 0.8 },
    { text: "Ambient pads", weight: 0.5 }
  ]
}

// Server → Client
{
  type: 'audio.chunk',
  chunk: 'base64_audio_data',
  timestamp: 1699999999
}

{
  type: 'prompt.filtered',
  promptId: 'prompt-3',
  reason: 'Inappropriate content'
}
```

### Frontend (React)

**Файлы:**
- `src/components/prompt-dj/PromptDJ.tsx` — главный UI компонент
- `src/utils/PromptDJHelper.ts` — WebSocket и Web Audio API логика
- `src/pages/workspace/PromptDJPage.tsx` — страница маршрута

**Поток данных:**

1. **Инициализация:**
   ```typescript
   const helper = new PromptDJHelper();
   await helper.connect(initialPrompts);
   ```

2. **Обновление промптов:**
   ```typescript
   helper.updatePrompts(updatedPrompts);
   ```

3. **Обработка аудио:**
   - WebSocket получает `base64` аудио (48kHz, 16-bit PCM, stereo)
   - Декодируется в `AudioBuffer`
   - Ставится в очередь через `AudioBufferSourceNode`
   - Бесшовное воспроизведение с точностью до миллисекунд

### Web Audio API Flow

```mermaid
graph LR
    WS[WebSocket] --> Decode[Base64 → Uint8Array]
    Decode --> PCM[PCM → AudioBuffer]
    PCM --> Source[AudioBufferSourceNode]
    Source --> Gain[GainNode]
    Gain --> Output[AudioContext.destination]
```

## ⚙️ Конфигурация

### Переменные окружения

**Required:**
- `GOOGLE_AI_API_KEY` — ключ API для Gemini Lyria (уже настроен в секретах Supabase)

### Audio Settings

**Формат аудио:**
- Sample Rate: **48 kHz**
- Bit Depth: **16-bit**
- Channels: **Stereo**
- Encoding: **PCM**

**Буферизация:**
- Initial Buffer: **2 секунды**
- WebSocket keepalive: **30 секунд**

## ⚠️ Важные замечания

### 1. Экспериментальная функция

Prompt DJ использует **Gemini Lyria API v1alpha** — экспериментальное API с возможными изменениями.

### 2. Требования

- ✅ Google AI API ключ (GOOGLE_AI_API_KEY)
- ✅ Современный браузер с Web Audio API
- ✅ Стабильное интернет-соединение

### 3. Ограничения

- ⚠️ API может быть нестабильным
- ⚠️ Некоторые промпты могут фильтроваться (content policy)
- ⚠️ Максимум 16 одновременных промптов

### 4. Производительность

**Оптимизации:**
- Throttling обновлений промптов (200 мс)
- Бесшовная очередь аудио для плавного воспроизведения
- Автоматическое восстановление после ошибок

## 🐛 Troubleshooting

### Ошибка подключения

**Симптомы:** "Не удалось запустить Prompt DJ"

**Решения:**
1. Проверьте наличие `GOOGLE_AI_API_KEY` в Supabase secrets
2. Убедитесь, что API ключ валиден
3. Проверьте консоль браузера на WebSocket ошибки

### Промпты не меняют музыку

**Причины:**
- Веса установлены в 0 (промпт неактивен)
- Промпт отфильтрован (проверьте красную рамку)
- WebSocket соединение прервано

**Решения:**
1. Увеличьте веса активных промптов
2. Перефразируйте отфильтрованные промпты
3. Перезапустите сессию

### Прерывистое воспроизведение

**Причины:**
- Медленное интернет-соединение
- Перегрузка CPU

**Решения:**
1. Уменьшите количество активных промптов
2. Закройте другие вкладки браузера
3. Увеличьте `bufferTime` в `PromptDJHelper.ts`

## 📊 Мониторинг

### Логи Edge Function

```bash
# Просмотр логов в Supabase Dashboard
Functions → prompt-dj-lyria-stream → Logs
```

**Ключевые события:**
- `[prompt-dj-lyria] Session created: <sessionId>`
- `[prompt-dj-lyria] WebSocket opened`
- `[prompt-dj-lyria] Updating prompts: <count>`
- `[prompt-dj-lyria] WebSocket closed`

### Browser Console

**Полезные логи:**
```javascript
// Frontend
[PromptDJHelper] Session created: <sessionId>
[PromptDJHelper] WebSocket connected
[PromptDJHelper] Prompts updated: <count>
[PromptDJHelper] Audio processing error: <error>
```

## 🔮 Будущие улучшения

### Планируется:

1. **Сохранение пресетов** — сохранять наборы промптов
2. **MIDI контроллер поддержка** — управление весами через MIDI-устройства
3. **Запись сессий** — экспорт микса в WAV/MP3
4. **Визуализация спектра** — частотный анализатор в реальном времени
5. **Совместная работа** — несколько пользователей микшируют одновременно
6. **AI-ассистент** — автоматические переходы между промптами

## 📚 Дополнительные ресурсы

**Документация:**
- [Gemini Lyria API Docs](https://ai.google.dev/gemini-api/docs/lyria)
- [Web Audio API Guide](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API)
- [WebSocket Protocol](https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API)

**Похожие проекты:**
- Google AI Test Kitchen — Prompt DJ demo
- Suno AI Chirp — multi-prompt music generation
- Stability AI Stable Audio — prompt-based music

---

**Версия:** 1.0.0  
**Дата:** 2025-11-02  
**Статус:** Экспериментальная функция
