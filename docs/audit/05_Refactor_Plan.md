# План рефакторинга и улучшений

**Дата:** 03.11.2025 (Версия 2, обновлено)

Этот документ содержит приоритезированный список задач, основанный на результатах аудита и новых требованиях.

---

## 🔥 Категория 1: Критические исправления и задачи (1-3 дня)

Эти задачи необходимо выполнить **в первую очередь**, так как они напрямую влияют на основной функционал, безопасность и стабильность приложения.

### **Задача 1.1: Исправить баг с отображением версий трека**

*   **Проблема:** Пользователи видят только одну версию трека, даже если сгенерировано несколько. Ключевой функционал не работает.
*   **Влияние:** **Критическое** (Функционал, UX).
*   **Решение:**
    1.  Провести диагностику фронтенд-кода (хук `useTrackVersions`, компоненты, кэш React Query).
    2.  Выявить и устранить причину некорректной фильтрации или отображения данных.
    3.  Убедиться, что все полученные от бэкенда версии трека отображаются в UI.
*   **Файлы для изменения (предположительно):**
    *   `src/hooks/useTrackVersions.ts`
    *   Компоненты, использующие этот хук (например, в `src/components/tracks/`).

### **Задача 1.2: Заменить интеграцию Mureka на Replicate.com**

*   **Проблема:** Необходимо мигрировать с Mureka на Replicate для анализа аудио. При этом **критически важно** обеспечить безопасность нового эндпоинта.
*   **Влияние:** **Критическое** (Безопасность, Функционал).
*   **Решение:**
    1.  Создать новый, безопасный API-клиент для Replicate в `supabase/functions/_shared/replicate.ts`.
    2.  Полностью переписать edge-функцию `supabase/functions/analyze-audio/index.ts`:
        *   **Добавить строгую аутентификацию пользователя** (проверку JWT-токена), как в `generate-suno`.
        *   Реализовать логику вызова API Replicate (запуск задачи, ожидание результата).
        *   Обработать возможные ошибки от API Replicate.
    3.  Удалить старый код, связанный с Mureka.
*   **Файлы для изменения:**
    *   `supabase/functions/analyze-audio/index.ts`
    *   (создать) `supabase/functions/_shared/replicate.ts`
    *   (удалить) упоминания Mureka

---

## 💧 Категория 2: Улучшение документации и UX (Параллельно)

Эти задачи направлены на повышение удобства работы с репозиторием.

### **Задача 2.1: Финальная полировка `README.md`**

*   **Проблема:** Главный `README.md` должен стать полноценной и удобной точкой входа в репозиторий.
*   **Влияние:** **Среднее** (DX - Developer Experience).
*   **Решение:**
    1.  Добавить в `README.md` больше элементов для наглядности: бейджи (статус сборки, лицензия), сворачиваемые секции `<details>` для длинных блоков кода.
    2.  Улучшить внутреннюю перелинковку между разделами и с другими важными документами (`ARCHITECTURE.md`, `CONTRIBUTING.md`).
*   **Файлы для изменения:**
    *   `README.md`

### **Задача 2.2: Визуализировать управление задачами**

*   **Проблема:** Отсутствует единая, наглядная страница для отслеживания прогресса по задачам и спринтам.
*   **Влияние:** **Среднее** (Прозрачность разработки).
*   **Решение:**
    1.  Проанализировать содержимое `project-management/`.
    2.  Создать/обновить `project-management/README.md` как главный дашборд.
    3.  Добавить на дашборд сводные таблицы по текущему спринту и диаграммы (например, диаграмму Ганта на Mermaid.js) для визуализации дорожной карты.
*   **Файлы для изменения:**
    *   (создать/обновить) `project-management/README.md`

---

## 🧊 Категория 3: Технический долг (Фоново)

### **Задача 3.1: Улучшить доступность (a11y) компонента `Button`**

*   **Проблема:** В состоянии `loading` компонент `Button` не имеет атрибута `aria-busy="true"`.
*   **Влияние:** **Низкое** (Доступность).
*   **Решение:**
    1.  В файле `src/components/ui/button.tsx` добавить в компонент `Comp` атрибут `aria-busy={loading}`.
*   **Файлы для изменения:**
    *   `src/components/ui/button.tsx`
