# ТЕХНИЧЕСКИЙ ДОЛГ И ПЛАН ДЕЙСТВИЙ
## Albert3 Muse Synth Studio - Комплексный аудит от 2025-11-28

---

## 📊 EXECUTIVE SUMMARY

Проведен комплексный аудит приложения Albert3 Muse Synth Studio по 6 ключевым направлениям:
1. **Логика приложения и структура данных**
2. **Мобильный интерфейс**
3. **Интеграции и API**
4. **Пользовательские хуки**
5. **Функционал приложения**
6. **Компоненты**

### Общая оценка: **B+ (7.5/10)**

**Сильные стороны:**
- ✅ Отличная архитектура с паттернами Repository, Provider, Service Layer
- ✅ Сильные оптимизации производительности (виртуализация, мемоизация, code splitting)
- ✅ Хорошая доступность (WCAG AAA touch targets, ARIA labels)
- ✅ Комплексная система обработки ошибок
- ✅ Mobile-first дизайн с поддержкой жестов

**Критические области для улучшения:**
- 🔴 5 критических уязвимостей безопасности
- 🔴 3 критических проблемы с хуками (риск бесконечных рендеров)
- 🔴 100+ использований типа `any` (потеря type safety)
- 🔴 3 отключенных компонента из-за проблем с типами
- 🔴 Незавершенная миграция от ApiService (538 LOC deprecated кода)

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (P0 - СРОЧНО)

### 1. БЕЗОПАСНОСТЬ

#### 1.1 Race Condition в счетчиках Repository
**Файл:** `src/repositories/SupabaseTrackRepository.ts:138-183`
**Проблема:** Неатомарные операции read-then-update для `incrementPlayCount`, `incrementLikeCount`, `decrementLikeCount`

```typescript
// ❌ ПЛОХО - текущая реализация
async incrementPlayCount(id: string): Promise<void> {
  const { data: track } = await supabase
    .from('tracks')
    .select('play_count')
    .eq('id', id)
    .single();

  if (track) {
    await supabase
      .from('tracks')
      .update({ play_count: (track.play_count || 0) + 1 })
      .eq('id', id);
  }
}

// ✅ ХОРОШО - использовать RPC
async incrementPlayCount(id: string): Promise<void> {
  const { error } = await supabase.rpc('increment_play_count', { track_id: id });
  if (error) throw error;
}
```

**Действие:** Использовать атомарные RPC функции (уже существуют в миграциях)
**Приоритет:** P0 - КРИТИЧНО
**Оценка:** 2-4 часа

---

#### 1.2 Опциональная проверка подписи вебхуков
**Файл:** `supabase/functions/suno-callback/index.ts:35-61`
**Проблема:** Проверка подписи вебхука опциональна, `suno-webhook` вообще не проверяет

```typescript
// ❌ ПЛОХО
if (SUNO_WEBHOOK_SECRET) {
  // Проверка только если секрет настроен
} else {
  logger.warn('SUNO_WEBHOOK_SECRET not configured - skipping verification');
}

// ✅ ХОРОШО
if (!SUNO_WEBHOOK_SECRET) {
  return new Response('Webhook secret not configured', { status: 500 });
}
// Всегда проверяем подпись
```

**Действие:** Сделать проверку обязательной для всех вебхук-хэндлеров
**Приоритет:** P0 - КРИТИЧНО
**Оценка:** 4-6 часов

---

#### 1.3 Rate Limiting можно обойти
**Файл:** `supabase/functions/_shared/rate-limit.ts:156-171`
**Проблема:** In-memory rate limiting не работает между инстансами Edge Functions

**Действие:**
1. Принудительно использовать Redis-based rate limiting
2. Настроить Upstash Redis в продакшене
3. Добавить database-level rate limiting как страховку

**Приоритет:** P0 - КРИТИЧНО
**Оценка:** 8-12 часов

---

#### 1.4 Отсутствует timeout для внешних API
**Файл:** `supabase/functions/_shared/suno.ts` (множественные вызовы)
**Проблема:** Fetch-запросы к внешним API без timeout

```typescript
// ❌ БЕЗ TIMEOUT
const response = await fetchImpl(endpoint, {
  method: "POST",
  headers: buildSunoHeaders(apiKey),
  body: JSON.stringify(apiPayload),
});

// ✅ С TIMEOUT
const response = await fetchImpl(endpoint, {
  method: "POST",
  headers: buildSunoHeaders(apiKey),
  body: JSON.stringify(apiPayload),
  signal: AbortSignal.timeout(30000), // 30 секунд
});
```

**Действие:** Добавить timeout ко всем внешним API вызовам
**Приоритет:** P0 - КРИТИЧНО
**Оценка:** 4-6 часов

---

#### 1.5 Нет аутентификации в generate-minimax
**Файл:** `supabase/functions/generate-minimax/index.ts`
**Проблема:** Edge функция не проверяет аутентификацию

**Действие:** Использовать `createAuthenticatedHandler` factory
**Приоритет:** P0 - КРИТИЧНО
**Оценка:** 1-2 часа

---

### 2. КРИТИЧЕСКИЕ БАГИ В ХУКАХ

#### 2.1 Re-render Storm в useVirtualizedList
**Файл:** `src/hooks/useVirtualizedList.ts:36-51`
**Проблема:** Вычисляемые значения пересчитываются на КАЖДЫЙ рендер

```typescript
// ❌ ПЛОХО
const startIndex = Math.max(0, Math.floor(scrollTop / itemHeight) - overscan);
const endIndex = Math.min(...);
const virtualItems = []; // пересоздается каждый раз

// ✅ ХОРОШО
const startIndex = useMemo(() =>
  Math.max(0, Math.floor(scrollTop / itemHeight) - overscan),
  [scrollTop, itemHeight, overscan]
);

const virtualItems = useMemo(() => {
  const items = [];
  for (let i = startIndex; i <= endIndex; i++) {
    items.push({...});
  }
  return items;
}, [startIndex, endIndex, items, itemHeight]);
```

**Действие:** Добавить useMemo для всех вычислений
**Приоритет:** P0 - КРИТИЧНО (влияет на производительность)
**Оценка:** 2-3 часа

---

#### 2.2 Unstable Dependency - риск бесконечных рендеров
**Файл:** `src/hooks/useDeleteTrack.ts:33-40`
**Проблема:** Зависимость от нестабильного объекта `mutation`

```typescript
// ❌ ПЛОХО
const deleteTrack = useCallback(
  (trackId: string) => {
    mutation.mutate(trackId);
  },
  [mutation] // mutation меняется каждый рендер!
);

// ✅ ХОРОШО
const deleteTrack = useCallback(
  (trackId: string) => {
    mutation.mutate(trackId);
  },
  [mutation.mutate] // Только стабильная функция mutate
);
```

**Действие:** Исправить зависимости во всех хуках с useMutation
**Приоритет:** P0 - КРИТИЧНО
**Оценка:** 3-4 часа

---

#### 2.3 Дублирование cleanup эффектов
**Файл:** `src/hooks/useGenerateMusic.ts:81-85, 460-464`
**Проблема:** Cleanup функция вызывается дважды

**Действие:** Удалить один из дублирующих эффектов
**Приоритет:** P0 - КРИТИЧНО
**Оценка:** 1 час

---

### 3. КРИТИЧЕСКИЕ ПРОБЛЕМЫ С ТИПАМИ

#### 3.1 Отключенные компоненты из-за type conflicts
**Файлы:**
- `src/components/tracks/TrackRowEnhanced.disabled.tsx`
- `src/features/tracks/ui/MinimalDetailPanel.disabled.tsx`
- `src/features/tracks/ui/ModernDetailPanel.disabled.tsx`

**Проблема:** useTrackState hook не предоставляет все необходимые свойства

**Действие:** Исправить типы в useTrackState и включить компоненты обратно
**Приоритет:** P0 - КРИТИЧНО (потеря функциональности)
**Оценка:** 6-8 часов

---

## 🟡 ВЫСОКОПРИОРИТЕТНЫЕ ПРОБЛЕМЫ (P1 - НА ЭТОЙ НЕДЕЛЕ)

### 4. TYPE SAFETY

#### 4.1 100+ использований `any`
**Критичные файлы:**
- `src/pages/workspace/LyricsLibrary.tsx` (7 instances)
- `src/pages/workspace/Library.tsx` (10+ instances)
- `src/types/edge-functions.ts` (множественные)
- `src/services/providers/adapters/suno.adapter.ts`

**Действие:** Систематическая замена `any` на правильные типы
**Приоритет:** P1
**Оценка:** 16-24 часа

---

### 5. DEPRECATED КОД

#### 5.1 ApiService (538 LOC) - незавершенная миграция
**Файл:** `src/services/api.service.ts`
**Проблема:** Помечен как deprecated, но всё ещё активно используется

**Действие:**
1. Завершить миграцию на domain-specific services
2. Или убрать пометку deprecated если миграция не планируется

**Приоритет:** P1
**Оценка:** 24-32 часа

---

#### 5.2 Дублирование LyricsService
**Файлы:**
- `src/services/lyrics.service.ts` (Timestamped lyrics)
- `src/services/lyrics/lyrics.service.ts` (Lyrics generation)

**Действие:** Переименовать в:
- `TimestampedLyricsService`
- `LyricsGenerationService`

**Приоритет:** P1
**Оценка:** 4-6 часов

---

### 6. КОНСОЛИДАЦИЯ ТИПОВ

#### 6.1 Track типы разбросаны по 5+ файлам
**Файлы с конфликтующими определениями:**
- `src/types/track.types.ts`
- `src/types/domain/track.types.ts`
- `src/types/providers.ts`
- `src/types/suno-ui.types.ts`
- `src/types/generation-status.ts`

**Конфликт:** TrackStatus определен 4 раза с разными значениями!

**Действие:** Консолидировать все в `src/types/track.types.ts`
**Приоритет:** P1
**Оценка:** 8-12 часов

---

### 7. БЕЗОПАСНОСТЬ - ПРОДОЛЖЕНИЕ

#### 7.1 Логирование чувствительных данных
**Файл:** `supabase/functions/_shared/suno.ts:497-503`

```typescript
// ❌ ПЛОХО
logger.debug('Suno API raw response', {
  bodyPreview: rawText.substring(0, 500), // может содержать токены!
});
```

**Действие:** Удалить логирование тел ответов в продакшене
**Приоритет:** P1
**Оценка:** 4-6 часов

---

#### 7.2 Валидация API ключей
**Проблема:** 52+ Edge Functions не проверяют наличие API ключей

**Действие:** Добавить обязательную проверку в начале каждой функции
**Приоритет:** P1
**Оценка:** 8-10 часов

---

### 8. ВЕБХУКИ

#### 8.1 Консолидация Suno webhook handlers
**Файлы:**
- `supabase/functions/suno-webhook/index.ts`
- `supabase/functions/suno-callback/index.ts`

**Действие:** Объединить в один хэндлер или четко разделить назначение
**Приоритет:** P1
**Оценка:** 6-8 часов

---

#### 8.2 Replicate webhook verification
**Проблема:** Нет обработчика вебхуков Replicate

**Действие:**
1. Создать replicate-callback функцию
2. Добавить проверку подписи
3. Добавить idempotency checks

**Приоритет:** P1
**Оценка:** 8-12 часов

---

### 9. ХУКИ - ИСПРАВЛЕНИЯ

#### 9.1 useLocalStorage - отсутствует memoization
**Файл:** `src/hooks/useLocalStorage.ts:21-33`

**Действие:** Обернуть setValue в useCallback
**Приоритет:** P1
**Оценка:** 1-2 часа

---

#### 9.2 useAudioRecorder - stale closure риск
**Файл:** `src/hooks/useAudioRecorder.ts:246-267`

**Действие:** Исправить cleanup эффект (использовать refs вместо state в зависимостях)
**Приоритет:** P1
**Оценка:** 2-3 часа

---

#### 9.3 useImageLazyLoad - memory leak
**Файл:** `src/hooks/useImageLazyLoad.ts:47-51`

**Действие:** Добавить `observer.disconnect()` в cleanup
**Приоритет:** P1
**Оценка:** 1 час

---

## 🟢 СРЕДНИЙ ПРИОРИТЕТ (P2 - СЛЕДУЮЩИЙ СПРИНТ)

### 10. МОБИЛЬНЫЙ ИНТЕРФЕЙС

#### 10.1 Стандартизация mobile detection hooks
**Проблема:** 46 файлов используют deprecated `useIsMobile()`

**Действие:** Мигрировать все на `useBreakpoints()`
**Приоритет:** P2
**Оценка:** 8-12 часов

---

#### 10.2 Native image lazy loading
**Проблема:** Отсутствует атрибут `loading="lazy"` на изображениях

**Действие:** Добавить `loading="lazy"` ко всем `<img>` элементам
**Приоритет:** P2
**Оценка:** 4-6 часов

---

#### 10.3 Консолидация bottom navigation
**Файлы:**
- `src/components/navigation/AppBottomNav.tsx`
- `src/components/navigation/EnhancedBottomNav.tsx`

**Действие:** Объединить в один компонент или четко разделить использование
**Приоритет:** P2
**Оценка:** 6-8 часов

---

#### 10.4 Touch targets < 44px
**Действие:** Проверить все кнопки и интерактивные элементы
**Приоритет:** P2
**Оценка:** 6-8 часов

---

### 11. API И ПРОИЗВОДИТЕЛЬНОСТЬ

#### 11.1 Стандартизация error response форматов
**Проблема:** 3 разных формата ответов от Edge Functions

**Действие:** Унифицировать в:
```typescript
interface SuccessResponse<T> {
  success: true;
  data: T;
  meta?: { timestamp: string; correlationId?: string; };
}
```

**Приоритет:** P2
**Оценка:** 8-12 часов

---

#### 11.2 Кэширование balance checks
**Файл:** `src/services/api.service.ts:458-537`

**Действие:** Добавить TTL-based кэширование (30 секунд)
**Приоритет:** P2
**Оценка:** 4-6 часов

---

#### 11.3 Оптимизация database queries
**Проблема:** Повсеместное использование `SELECT *`

**Действие:** Создать view-specific queries с выборкой только нужных полей
**Приоритет:** P2
**Оценка:** 12-16 часов

---

#### 11.4 Рефакторинг suno.ts (1134 строки!)
**Действие:** Разбить на модули:
- `suno-client.ts`
- `suno-generation.ts`
- `suno-lyrics.ts`
- `suno-stems.ts`
- `suno-utils.ts`

**Приоритет:** P2
**Оценка:** 16-20 часов

---

### 12. ТЕСТИРОВАНИЕ

#### 12.1 Service layer tests
**Проблема:** Только 1 тестовый файл из 31 сервиса

**Действие:** Создать тесты для:
- TrackService
- GenerationService
- LyricsService
- ProviderFactory

**Приоритет:** P2
**Оценка:** 24-32 часа

---

#### 12.2 Mobile E2E tests
**Действие:** Создать Playwright тесты для:
- Bottom navigation
- Swipe gestures
- Player controls
- Touch interactions

**Приоритет:** P2
**Оценка:** 16-24 часа

---

### 13. КОД КАЧЕСТВО

#### 13.1 Рефакторинг useTracks
**Файл:** `src/hooks/useTracks.ts` (7 useEffect!)

**Действие:** Разбить на:
- `useTracksQuery`
- `useTracksPolling`
- `useTracksRealtime`
- `useTracksStuckCheck`

**Приоритет:** P2
**Оценка:** 8-12 часов

---

#### 13.2 ESLint disable comments
**Проблема:** 17 instances eslint-disable

**Действие:** Исправить underlying issues вместо подавления
**Приоритет:** P2
**Оценка:** 8-10 часов

---

## ⚪ НИЗКИЙ ПРИОРИТЕТ (P3 - ТЕХДОЛГ)

### 14. ДОПОЛНИТЕЛЬНЫЕ УЛУЧШЕНИЯ

#### 14.1 Sentry Edge integration
**Действие:** Реализовать Sentry SDK для Edge Functions
**Приоритет:** P3
**Оценка:** 8-12 часов

---

#### 14.2 PWA features
**Действие:**
- Service Worker
- Web App Manifest
- Offline support
- Install prompt

**Приоритет:** P3
**Оценка:** 24-32 часа

---

#### 14.3 Query key factory
**Действие:** Централизованная фабрика query keys для всех entities
**Приоритет:** P3
**Оценка:** 6-8 часов

---

#### 14.4 Документация миграций
**Действие:** Документировать пути миграции от deprecated кода
**Приоритет:** P3
**Оценка:** 8-12 часов

---

## 📅 ROADMAP

### НЕДЕЛЯ 1-2 (P0 - КРИТИЧНО)
**Фокус:** Безопасность и критические баги

- [ ] Race conditions в repository (2-4ч)
- [ ] Webhook signature verification (4-6ч)
- [ ] Redis rate limiting (8-12ч)
- [ ] API timeouts (4-6ч)
- [ ] generate-minimax auth (1-2ч)
- [ ] useVirtualizedList fix (2-3ч)
- [ ] useDeleteTrack fix (3-4ч)
- [ ] useGenerateMusic cleanup (1ч)
- [ ] Fix useTrackState types (6-8ч)

**Итого:** ~40-58 часов (~1-1.5 недели для 1 разработчика)

---

### НЕДЕЛЯ 3-4 (P1 - ВЫСОКИЙ ПРИОРИТЕТ)
**Фокус:** Type safety и deprecated код

- [ ] Replace 100+ any types (16-24ч)
- [ ] ApiService migration (24-32ч)
- [ ] LyricsService rename (4-6ч)
- [ ] Track types consolidation (8-12ч)
- [ ] Log sanitization (4-6ч)
- [ ] API key validation (8-10ч)
- [ ] Suno webhooks consolidation (6-8ч)
- [ ] Replicate webhook handler (8-12ч)
- [ ] Hook fixes (4-6ч)

**Итого:** ~82-116 часов (~2-3 недели)

---

### НЕДЕЛЯ 5-8 (P2 - СРЕДНИЙ ПРИОРИТЕТ)
**Фокус:** Мобильный интерфейс и производительность

- [ ] Mobile hooks migration (8-12ч)
- [ ] Native lazy loading (4-6ч)
- [ ] Bottom nav consolidation (6-8ч)
- [ ] Touch targets audit (6-8ч)
- [ ] Error response formats (8-12ч)
- [ ] Balance caching (4-6ч)
- [ ] Query optimization (12-16ч)
- [ ] suno.ts refactor (16-20ч)
- [ ] Service tests (24-32ч)
- [ ] Mobile E2E tests (16-24ч)
- [ ] useTracks refactor (8-12ч)
- [ ] ESLint fixes (8-10ч)

**Итого:** ~120-166 часов (~3-4 недели)

---

### НЕДЕЛЯ 9+ (P3 - ТЕХДОЛГ)
**Фокус:** Долгосрочные улучшения

- [ ] Sentry integration (8-12ч)
- [ ] PWA features (24-32ч)
- [ ] Query key factory (6-8ч)
- [ ] Migration docs (8-12ч)

**Итого:** ~46-64 часа (~1-1.5 недели)

---

## 📊 ОБЩАЯ ОЦЕНКА

| Приоритет | Задач | Время (часы) | Команда | Календарь |
|-----------|-------|--------------|---------|-----------|
| P0 - Критично | 9 | 40-58 | 1 dev | 1-1.5 недели |
| P1 - Высокий | 9 | 82-116 | 1 dev | 2-3 недели |
| P2 - Средний | 12 | 120-166 | 1-2 devs | 3-4 недели |
| P3 - Низкий | 4 | 46-64 | 1 dev | 1-1.5 недели |
| **ВСЕГО** | **34** | **288-404** | **1-2 devs** | **7-10 недель** |

---

## 🎯 РЕКОМЕНДАЦИИ

### Немедленные действия (сегодня):
1. ✅ Создать JIRA/GitHub issues для всех P0 задач
2. ✅ Назначить владельца для каждой критической задачи
3. ✅ Запланировать daily standups для отслеживания прогресса

### Эта неделя:
1. ✅ Завершить все P0 задачи
2. ✅ Начать P1 задачи (type safety, deprecated code)
3. ✅ Code review для всех критических фиксов

### Этот месяц:
1. ✅ Завершить P0 и P1
2. ✅ Начать P2 (mobile, performance)
3. ✅ Настроить автоматизированное тестирование

### Следующие 2 месяца:
1. ✅ Завершить P2
2. ✅ Начать P3 (PWA, документация)
3. ✅ Провести повторный аудит

---

## 📝 ЗАМЕТКИ

### Положительные моменты:
- Отличная архитектура (Repository, Provider, Service Layer)
- Сильные performance optimizations
- Хорошая accessibility (WCAG AAA)
- Комплексная система обработки ошибок
- Mobile-first дизайн

### Главные риски:
- Критические уязвимости безопасности (webhook, rate limiting)
- Потеря type safety (100+ any)
- Отключенные компоненты (потеря функциональности)
- Незавершенная миграция (ApiService)

### Рекомендуемая команда:
- 1-2 Senior/Mid разработчика для P0-P1
- 1 разработчик для P2-P3
- QA инженер для тестирования
- DevOps для настройки Redis, Sentry

---

**Дата создания:** 2025-11-28
**Автор:** Claude (AI Code Assistant)
**Версия:** 1.0
**Статус:** Готово к реализации
