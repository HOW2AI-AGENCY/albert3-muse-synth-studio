# План дальнейших работ - Albert3 Muse Synth Studio

**Дата составления:** 04 ноября 2025, 08:00 UTC
**Статус:** Актуально
**Приоритизация:** На основе результатов аудита от 04.11.2025

---

## 📊 Текущая ситуация

### Выполнено (P1 - Критичные)
- [x] ✅ Замена console.* на logger (25 замен в 9 файлах)
- [x] ✅ Обновление зависимостей (vite 7.1.12, supabase 2.56.0)
- [x] ✅ Ограничение CORS (localhost whitelist + документация)
- [x] ✅ Настройка CSP headers (Content-Security-Policy)

**Результат:** Безопасность 8.0/10 → 9.0/10 ⭐

### В работе
- [ ] 🔄 Добавление тестов для критичных компонентов

---

## 🎯 Краткосрочный план (2-3 недели)

### Неделя 1: Завершение P1 - Тестирование

#### 1. Тесты для Library.tsx (3-4 часа) 🔴
**Приоритет:** Критический
**Ответственный:** Frontend Developer

**Что протестировать:**
- Рендеринг списка треков
- Фильтрация и поиск
- Сортировка
- Удаление треков
- Открытие диалогов (SeparateStems, Extend, etc.)

**Результат:**
- Покрытие: 0% → 80%
- Предотвращение регрессий при рефакторинге

#### 2. Тесты для DetailPanelContent.tsx (3-4 часа) 🔴
**Приоритет:** Критический
**Ответственный:** Frontend Developer

**Что протестировать:**
- Отображение деталей трека
- Редактирование метаданных (title, tags, etc.)
- Создание версий
- Управление тегами

**Результат:**
- Покрытие: 0% → 80%
- Защита от багов при обновлении UI

#### 3. Тесты для MusicGeneratorContent.tsx (2 часа) 🟡
**Приоритет:** Высокий
**Ответственный:** Frontend Developer

**Что протестировать:**
- Рендеринг формы
- Валидация полей
- Отправка запроса на генерацию
- Обработка ошибок

**Результат:**
- Покрытие: 0% → 80%
- Улучшение UX при ошибках

**Итого Неделя 1:** 8-10 часов

---

### Неделя 2-3: P2 - Высокие приоритеты

#### 4. Рефакторинг Library.tsx (12-16 часов) 🟡
**Приоритет:** Высокий
**Ответственный:** Senior Frontend Developer

**Текущая проблема:**
- 831 строка в одном файле
- 12+ state переменных
- Частые ре-рендеры всего компонента
- Сложность поддержки

**План рефакторинга:**

**Шаг 1: Вынести фильтры (2-3ч)**
```
Library.tsx (831 строк)
  ↓
LibraryFilters.tsx (80 строк)
  - SearchInput
  - SortSelect
  - StatusFilter
  - ViewModeToggle
```

**Шаг 2: Вынести диалоги (3-4ч)**
```
LibraryDialogs.tsx (200 строк)
  - SeparateStemsDialog
  - ExtendDialog
  - CoverDialog
  - RemixDialog
  - PersonaDialog
```

**Шаг 3: Создать custom hook (2-3ч)**
```typescript
// useLibraryState.ts (100 строк)
export const useLibraryState = () => {
  const [filters, setFilters] = useState<LibraryFilters>({...});
  const [dialogs, setDialogs] = useState<LibraryDialogs>({...});

  return {
    filters,
    setFilters,
    dialogs,
    openDialog,
    closeDialog
  };
};
```

**Шаг 4: Создать LibraryContent (2-3ч)**
```
LibraryContent.tsx (150 строк)
  - VirtualizedTrackList
  - TrackCard (memoized)
  - EmptyState
```

**Шаг 5: Обновить главный компонент (2-3ч)**
```typescript
// Library.tsx (150 строк)
export const Library = () => {
  const { filters, setFilters, dialogs, openDialog, closeDialog } = useLibraryState();
  const { data: tracks, isLoading } = useTracks(filters);

  return (
    <PageContainer>
      <LibraryFilters {...filters} onFiltersChange={setFilters} />
      <LibraryContent tracks={tracks} isLoading={isLoading} />
      <LibraryDialogs dialogs={dialogs} onClose={closeDialog} />
    </PageContainer>
  );
};
```

**Ожидаемый результат:**
- Размер файла: 831 → 150 строк
- Производительность: +15-20% (меньше ре-рендеров)
- Поддерживаемость: +50%
- Тестируемость: +100%

#### 5. Замена `any` на типы (6-8 часов) 🟡
**Приоритет:** Высокий
**Ответственный:** TypeScript Expert

**Текущая проблема:**
- 118 использований `any`
- Потеря type safety
- Сложность рефакторинга

**План:**

**Шаг 1: Создать недостающие интерфейсы (2-3ч)**
```typescript
// src/types/api-responses.types.ts
export interface TrackUpdate {
  id: string;
  title: string;
  status: TrackStatus;
  audio_url: string | null;
  cover_url: string | null;
  // ... все поля
}

export interface WebhookPayload<T = unknown> {
  event: 'INSERT' | 'UPDATE' | 'DELETE';
  new: T;
  old: T;
}

export interface SunoGenerateResponse {
  task_id: string;
  status: 'pending' | 'processing' | 'completed' | 'failed';
  // ... все поля
}
```

**Шаг 2: Заменить any в error handling (2ч)**
```typescript
// ❌ До
} catch (error: any) {
  console.error('Error:', error);
}

// ✅ После
} catch (error) {
  const errorMessage = error instanceof Error
    ? error.message
    : 'Unknown error';
  logger.error('Operation failed', error);
}
```

**Шаг 3: Заменить any в API responses (2-3ч)**
```typescript
// ❌ До
const track = payload.new as any;

// ✅ После
const track = payload.new as TrackUpdate;
```

**Шаг 4: Включить строгую проверку (1ч)**
```json
// tsconfig.json
{
  "compilerOptions": {
    "noImplicitAny": true,
    "@typescript-eslint/no-explicit-any": "error"
  }
}
```

**Ожидаемый результат:**
- `any` usage: 118 → <20 (-83%)
- Type safety: +100%
- IDE autocomplete: Улучшение

#### 6. Добавить retry механизм для API (4-6 часов) 🟢
**Приоритет:** Средний
**Ответственный:** Backend Developer

**Текущая проблема:**
- При временных сбоях API запросы сразу падают
- Плохой UX при нестабильной сети

**План:**

**Шаг 1: Создать fetchWithRetry утилиту (2-3ч)**
```typescript
// src/utils/fetch-with-retry.ts
interface RetryConfig {
  maxRetries?: number;
  baseDelay?: number;
  maxDelay?: number;
  retryableStatuses?: number[];
}

export const fetchWithRetry = async <T>(
  url: string,
  options: RequestInit = {},
  config: RetryConfig = {}
): Promise<T> => {
  const { maxRetries = 3, baseDelay = 1000, maxDelay = 10000 } = config;

  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      const response = await fetch(url, options);
      if (response.ok) return await response.json();

      if (attempt === maxRetries) throw new Error(`Max retries exceeded`);

      // Exponential backoff с jitter
      const delay = Math.min(
        baseDelay * Math.pow(2, attempt) + Math.random() * 1000,
        maxDelay
      );

      await sleep(delay);
    } catch (error) {
      if (attempt === maxRetries) throw error;
    }
  }
};
```

**Шаг 2: Интегрировать в Edge функции (2-3ч)**
```typescript
// supabase/functions/_shared/fetch-with-retry.ts
export const fetchSunoWithRetry = async (url: string) => {
  return fetchWithRetry<SunoResponse>(url, {
    headers: buildSunoHeaders(),
  }, {
    maxRetries: 3,
    retryableStatuses: [408, 429, 500, 502, 503, 504]
  });
};
```

**Ожидаемый результат:**
- API resilience: +50%
- User experience: Лучше при нестабильной сети
- Меньше ошибок в production

#### 7. Настроить Dependabot (2 часа) 🟢
**Приоритет:** Средний
**Ответственный:** DevOps

**План:**

```yaml
# .github/dependabot.yml
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    open-pull-requests-limit: 10
    reviewers:
      - "team-leads"
    labels:
      - "dependencies"
      - "security"
    groups:
      development-dependencies:
        dependency-type: "development"
        update-types: ["minor", "patch"]
      production-dependencies:
        dependency-type: "production"
        update-types: ["patch"]
```

**Ожидаемый результат:**
- Автоматические PR для обновлений
- Раннее обнаружение уязвимостей
- Меньше технического долга

**Итого Неделя 2-3:** 24-32 часа

---

## 🚀 Среднесрочный план (1-2 месяца)

### P3 - Средние приоритеты

#### 8. Стабилизировать E2E тесты (8-10 часов) 🟢
**Приоритет:** Средний

**Проблема:**
- E2E тесты нестабильны
- Требуют sudo для запуска Supabase

**Решение: Docker Compose**
```yaml
# docker-compose.test.yml
version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: albert3_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
```

**Результат:**
- Стабильные E2E тесты
- Легкий запуск в CI/CD
- Не требует sudo

#### 9. Добавить Lighthouse CI (3 часа) 🟢
**Приоритет:** Средний

**План:**
```yaml
# .github/workflows/lighthouse.yml
name: Lighthouse CI
on:
  pull_request:
    branches: [main]
jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: treosh/lighthouse-ci-action@v9
        with:
          urls: |
            http://localhost:5173
            http://localhost:5173/workspace/library
```

**Результат:**
- Автоматический контроль Web Vitals
- Performance budget enforcement
- Предотвращение деградации производительности

#### 10. Оптимизировать производительность Library (4-6 часов) 🟢
**Приоритет:** Средний

**Задачи:**
- Внедрить виртуализацию для списка треков
- Оптимизировать фильтрацию (Web Worker)
- Добавить дебаунс для поиска
- Lazy loading для изображений

**Результат:**
- Улучшение FPS: 30-40 → 60
- Уменьшение времени рендера: -50%
- Лучший UX при большом количестве треков

---

## 📈 Метрики успеха

### После выполнения P1 + P2

| Метрика | Сейчас | Цель | Улучшение |
|---------|--------|------|-----------|
| **Общая оценка** | 7.7/10 | 8.5/10 | +10% |
| **Безопасность** | 9.0/10 | 9.0/10 | ✅ |
| **Качество кода** | 7.5/10 | 8.5/10 | +13% |
| **Тестирование** | 6.5/10 | 8.0/10 | +23% |
| **Производительность** | 7.5/10 | 8.0/10 | +7% |
| **Покрытие тестами** | 65-75% | 80-85% | +10-15% |
| **Использование any** | 118 | <20 | -83% |
| **Размер Library.tsx** | 831 строк | 150 строк | -82% |

### После выполнения P1 + P2 + P3

| Метрика | Сейчас | Цель | Улучшение |
|---------|--------|------|-----------|
| **Общая оценка** | 7.7/10 | 9.0/10 | +17% |
| **Производительность** | 7.5/10 | 8.5/10 | +13% |
| **E2E тесты** | Нестабильны | Стабильны | ✅ |
| **Web Vitals** | Не контролируются | Автоматический контроль | ✅ |

---

## 💼 Распределение ресурсов

### Команда

| Роль | Задачи | Недели |
|------|--------|--------|
| **Frontend Developer** | Тесты Library, DetailPanel, Generator | 1 |
| **Senior Frontend** | Рефакторинг Library.tsx | 2-3 |
| **TypeScript Expert** | Замена any на типы | 2-3 |
| **Backend Developer** | Retry механизм | 2-3 |
| **DevOps** | Dependabot, Lighthouse CI | 2-3 |
| **QA** | E2E тесты stabilization | 4-6 |

### Временные затраты

| Период | Задачи | Часы |
|--------|--------|------|
| **Неделя 1** | Тестирование критичных компонентов | 8-10 |
| **Недели 2-3** | Рефакторинг + Типизация + Retry + Dependabot | 24-32 |
| **Месяцы 2-3** | E2E + Lighthouse + Performance | 15-19 |
| **Итого** | | 47-61 час |

---

## 🎯 Следующие шаги (Immediate Actions)

### Эта неделя (04-10 ноября)

1. **Вторник-Среда (04-05.11):**
   - [ ] Написать тесты для Library.tsx (3-4ч)
   - [ ] Code review для тестов

2. **Четверг-Пятница (06-07.11):**
   - [ ] Написать тесты для DetailPanelContent.tsx (3-4ч)
   - [ ] Code review для тестов

3. **Понедельник (10.11):**
   - [ ] Написать тесты для MusicGeneratorContent.tsx (2ч)
   - [ ] Запустить полный тестовый набор
   - [ ] Проверить покрытие (должно быть 80%+)

### Следующая неделя (11-17 ноября)

1. **Понедельник-Вторник (11-12.11):**
   - [ ] Начать рефакторинг Library.tsx
   - [ ] Вынести LibraryFilters

2. **Среда-Четверг (13-14.11):**
   - [ ] Вынести LibraryDialogs
   - [ ] Создать useLibraryState hook

3. **Пятница (15.11):**
   - [ ] Обновить главный Library компонент
   - [ ] Code review

---

## 📊 Отслеживание прогресса

### Dashboard Metrics

Создать dashboard для отслеживания:
- ✅ Покрытие тестами (target: 80%)
- ✅ Количество `any` (target: <20)
- ✅ Размер файлов (target: <400 строк)
- ✅ Web Vitals (LCP, FID, CLS)
- ✅ Bundle size
- ✅ Уязвимости в зависимостях (target: 0)

### Weekly Status Updates

Каждую пятницу:
- Обновлять `2025-11-04_Implementation_Status.md`
- Отчёт о выполненных задачах
- Блокеры и риски
- План на следующую неделю

---

## ⚠️ Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Рефакторинг Library.tsx сломает функционал | Средняя | Высокое | Полное покрытие тестами до рефакторинга |
| Обновление Vite 7 вызовет breaking changes | Низкая | Среднее | Тщательное тестирование после npm install |
| CORS ограничение сломает production | Низкая | Высокое | Настроить CORS_ALLOWED_ORIGINS перед deploy |
| Недостаток времени на тестирование | Средняя | Среднее | Приоритизация критичных компонентов |

---

## 📚 Документация

### Обновить документацию

- [ ] README.md - добавить раздел "Последние обновления"
- [ ] ARCHITECTURE.md - обновить после рефакторинга
- [ ] TESTING.md - добавить примеры тестов
- [ ] PERFORMANCE.md - документировать оптимизации

---

## 🎊 Итоговая цель

К **01 декабря 2025**:
- ✅ Все задачи P1 и P2 выполнены
- ✅ Покрытие тестами: 80%+
- ✅ Оценка проекта: 8.5/10
- ✅ 0 уязвимостей в зависимостях
- ✅ Library.tsx отрефакторен
- ✅ Все `any` заменены на типы

---

**Подготовлено:** Claude AI (Sonnet 4.5)
**Дата:** 04 ноября 2025, 08:00 UTC
**Следующее обновление:** 11 ноября 2025
