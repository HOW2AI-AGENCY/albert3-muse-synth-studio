# 📊 Отчет по аудиту мобильного интерфейса

**Дата**: 2025-11-05
**Версия**: 1.0.0
**Статус**: 🔴 Критические проблемы обнаружены

---

## 🎯 Обзор

Проведен полный аудит мобильного интерфейса для выявления конфликтов позиционирования, z-index и проблем адаптации на мобильных устройствах.

---

## 🔴 Критические проблемы

### 1. Конфликт систем Z-INDEX

**Обнаружено**: Две конфликтующие системы z-index в проекте

#### Система A: `src/styles/design-tokens.css` (строки 197-211)
```css
--z-sidebar: 40;
--z-bottom-tab-bar: 40;         /* ⚠️ КОНФЛИКТ */
--z-fab: 50;
--z-mini-player: 60;
--z-desktop-player: 60;
--z-dropdown: 1000;
--z-sticky: 1020;
--z-fixed: 1030;
--z-modal-backdrop: 1040;
--z-modal: 1050;
--z-fullscreen-player: 1050;
--z-popover: 1060;
--z-tooltip: 1070;
--z-toast: 1080;
--z-maximum: 2147483647;
```

#### Система B: `src/index.css` (строки 73-78)
```css
--z-dropdown: 50;               /* ⚠️ КОНФЛИКТ с 1000 */
--z-sticky: 100;                /* ⚠️ КОНФЛИКТ с 1020 */
--z-drawer: 200;
--z-player: 300;                /* ⚠️ КОНФЛИКТ с 60 */
--z-modal: 400;                 /* ⚠️ КОНФЛИКТ с 1050 */
--z-toast: 500;                 /* ⚠️ КОНФЛИКТ с 1080 */
```

**Проблема**: Разные файлы определяют разные значения для одних и тех же слоев.

---

### 2. Несогласованное использование Z-INDEX в компонентах

#### MobileNavigation.tsx (строки 186, 199, 238)
```tsx
// Overlay
className="... z-40 ..."           // ❌ Hardcoded

// Side Drawer
className="... z-50 ..."           // ❌ Hardcoded

// Bottom Tab Bar
className="... z-30 ..."           // ❌ Hardcoded, НЕ СООТВЕТСТВУЕТ --z-bottom-tab-bar: 40
```

#### BottomTabBar.tsx (строка 71)
```tsx
style={{ zIndex: 'var(--z-bottom-tab-bar)' }}  // ✅ Правильно использует переменную
// Но значение --z-bottom-tab-bar = 40
```

#### MiniPlayer.tsx (строка 80)
```tsx
className="... z-mini-player ..."   // ❌ ОШИБКА: пытается использовать как Tailwind класс
// Правильное значение: --z-mini-player = 60
```

---

### 3. Перекрытие слоев: Мини-плеер и нижняя навигация

**Текущая ситуация**:
```
z-30  ← Bottom Tab Bar (MobileNavigation - hardcoded)
z-40  ← Bottom Tab Bar (BottomTabBar - из переменной)
z-60  ← Mini Player (из design-tokens, НО неправильно применено)
```

**Проблема**:
- `MiniPlayer.tsx` использует `className="z-mini-player"` вместо `style={{ zIndex: 'var(--z-mini-player)' }}`
- Tailwind не знает о классе `z-mini-player`, поэтому z-index не применяется
- В результате: мини-плеер может перекрываться навигацией

---

### 4. Разные подходы к Safe Area Insets

#### MiniPlayer.tsx (строки 80-84)
```tsx
className="... safe-area-bottom ..."
style={{
  paddingBottom: 'calc(env(safe-area-inset-bottom) + var(--mobile-spacing-md))',
  paddingTop: 'var(--mobile-spacing-md)'
}}
```

#### BottomTabBar.tsx (строка 67)
```tsx
className="pb-[env(safe-area-inset-bottom)]"
```

**Проблема**: Несогласованные подходы к обработке safe-area. MiniPlayer добавляет дополнительный padding, BottomTabBar - только базовый.

---

### 5. Отсутствие координации между Fixed элементами

**Все три компонента используют `position: fixed` с `bottom: 0`**:

1. **MobileNavigation.tsx**: Bottom Tab Bar (line 238)
   ```tsx
   className="fixed bottom-0 left-0 right-0 z-30"
   ```

2. **BottomTabBar.tsx**: Tab Bar (line 62-64)
   ```tsx
   className="fixed bottom-0 left-0 right-0"
   style={{ zIndex: 'var(--z-bottom-tab-bar)' }}
   ```

3. **MiniPlayer.tsx**: Player (line 80)
   ```tsx
   className="fixed bottom-0 left-0 right-0 z-mini-player"
   ```

**Результат**: Все три элемента располагаются в одном месте, но с неправильными z-index значениями, что вызывает перекрытия.

---

## ⚠️ Средние проблемы

### 6. CompactCustomForm: Неоптимальное позиционирование footer

**Файл**: `src/components/generator/forms/CompactCustomForm.tsx` (строка 642)

```tsx
<div className="absolute bottom-0 left-0 right-0 border-t ...">
```

**Проблема**:
- Использует `absolute` вместо `fixed`
- Footer привязан к родительскому контейнеру, а не к viewport
- Может скрываться при прокрутке формы

**Рекомендация**: Рассмотреть использование `sticky` позиционирования для footer.

---

### 7. TrackCardMobile: Touch targets меньше минимального

**Файл**: `src/features/tracks/components/TrackCardMobile.tsx` (строки 110, 123, 131)

```tsx
className="h-7 w-7 p-0 mobile-touch"  // 28px × 28px
```

**Проблема**:
- Минимальный размер: 44px (WCAG AAA)
- Текущий размер кнопок: 28px
- Нарушение стандартов доступности

---

## ✅ Правильные реализации

### 8. Использование CSS переменных для spacing

**BottomTabBar.tsx** (строки 109-110):
```tsx
"gap-[var(--mobile-spacing-xs)]",
"px-[var(--mobile-spacing-sm)] py-[var(--mobile-spacing-sm)]",
```

**Оценка**: ✅ Отлично - использует дизайн-токены

---

### 9. Keyboard navigation в BottomTabBar

**BottomTabBar.tsx** (строки 74-89):
```tsx
onKeyDown={(e) => {
  // Arrow navigation между табами
}}
```

**Оценка**: ✅ Хорошая доступность

---

## 🎨 Рекомендации по z-index системе

### Предлагаемая единая шкала:

```css
/* ===== UNIFIED Z-INDEX SCALE ===== */
:root {
  /* Base layers (0-99) */
  --z-base: 0;
  --z-sidebar: 40;
  --z-bottom-nav: 50;            /* ← Единый для всех навигаций */

  /* Interactive layers (100-999) */
  --z-mini-player: 60;           /* Выше навигации */
  --z-fab: 70;
  --z-dropdown: 100;
  --z-sticky: 110;
  --z-drawer: 200;

  /* Overlay layers (1000+) */
  --z-modal-backdrop: 1000;
  --z-modal: 1010;
  --z-sheet: 1020;              /* Bottom sheets */
  --z-popover: 1030;
  --z-tooltip: 1040;
  --z-toast: 1050;
  --z-maximum: 2147483647;
}
```

---

## 📋 План исправлений (приоритеты)

### P0 - Критические (немедленно)

1. ✅ **Унифицировать z-index систему**
   - Удалить дублирующую систему из `index.css`
   - Использовать только `design-tokens.css`

2. ✅ **Исправить MiniPlayer z-index**
   - Заменить `className="z-mini-player"` на `style={{ zIndex: 'var(--z-mini-player)' }}`

3. ✅ **Согласовать MobileNavigation z-index**
   - Заменить hardcoded значения на CSS переменные

### P1 - Высокий приоритет

4. ✅ **Стандартизировать safe-area handling**
   - Единый подход для всех fixed компонентов

5. ✅ **Увеличить touch targets в TrackCardMobile**
   - Минимум 44px для всех кнопок

### P2 - Средний приоритет

6. ⏳ **Оптимизировать CompactCustomForm footer**
   - Рассмотреть sticky позиционирование

7. ⏳ **Добавить CSS-класс для z-index в Tailwind конфигурацию**
   - Для удобства использования

---

## 📊 Матрица тестирования устройств

### iOS (Safari)
| Устройство | Ширина | Высота | Safe Area | Статус |
|-----------|--------|---------|-----------|--------|
| iPhone SE 2 | 375px | 667px | ✅ | 🔴 Не тестировано |
| iPhone 13 | 390px | 844px | ✅ | 🔴 Не тестировано |
| iPhone 14 Pro Max | 430px | 932px | ✅ | 🔴 Не тестировано |
| iPad Mini | 768px | 1024px | ✅ | 🔴 Не тестировано |

### Android (Chrome)
| Устройство | Ширина | Высота | Safe Area | Статус |
|-----------|--------|---------|-----------|--------|
| Pixel 5 | 393px | 851px | ⚠️ | 🔴 Не тестировано |
| Samsung S20 | 360px | 800px | ⚠️ | 🔴 Не тестировано |
| Samsung S22 | 384px | 854px | ⚠️ | 🔴 Не тестировано |

---

## 🔧 Инструменты для тестирования

### Chrome DevTools
- Device mode с конкретными профилями устройств
- Network throttling (3G/4G)
- Touch simulation

### Lighthouse Mobile Audit
```bash
npx lighthouse http://localhost:8080 --preset=desktop --view
```

### Playwright для E2E
```bash
npm run test:e2e
```

---

## 📝 Следующие шаги

1. ✅ **Этап 1 (текущий)**: Анализ завершен
2. ⏭️ **Этап 2**: Внедрение единой z-index системы
3. ⏭️ **Этап 3**: Исправление компонентов
4. ⏭️ **Этап 4**: Тестирование на реальных устройствах
5. ⏭️ **Этап 5**: Документирование изменений

---

## 📚 Ссылки

- [WCAG 2.1 Touch Target Guidelines](https://www.w3.org/WAI/WCAG21/Understanding/target-size.html)
- [iOS Safe Area Insets](https://developer.apple.com/design/human-interface-guidelines/layout)
- [Material Design Touch Targets](https://m3.material.io/foundations/interaction/touch-targets)
- [CSS z-index Best Practices](https://developer.mozilla.org/en-US/docs/Web/CSS/z-index)

---

**Составил**: Claude Code
**Следующий обзор**: После внедрения исправлений
