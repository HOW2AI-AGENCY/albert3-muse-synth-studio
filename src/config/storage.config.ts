/**
 * @fileoverview Centralized configuration for Supabase Storage buckets.
 */

export const STORAGE_BUCKETS = {
  IMAGES: 'images',
  AUDIO: 'tracks-audio',
  COVERS: 'tracks-covers',
  VIDEOS: 'tracks-videos',
  PROJECT_COVERS: 'projects-covers',
};
